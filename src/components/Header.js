import React from 'react';

//contains the text in header

const Header = props => (
    <header className="header">
        <h3 className="title">Lock the Day</h3>
        <p>Don't forget to wish your loved ones on their birthday!!</p>
        <p>Let's Make a Birthday Log</p>
    </header>
);

export default Header;